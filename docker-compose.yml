version: '3.8'

services:
  crawler:
    build:
      context: ./crawler
    volumes:
      - datalake:/data/datalake
      - metadata:/data/metadata
    command: ["java", "-jar", "crawler-1.0-SNAPSHOT.jar"]

  indexer:
    build:
      context: ./indexer
    volumes:
      - datalake:/data/datalake
      - datamart:/data/datamart
    command: ["java", "-jar", "indexer-1.0-SNAPSHOT.jar"]

  query-engine:
    build:
      context: ./query-engine
    volumes:
      - datalake:/data/datalake
      - metadata:/data/metadata
      - datamart:/data/datamart
    command: ["java", "-jar", "query-engine-1.0-SNAPSHOT.jar"]

volumes:
  datalake:
  metadata:
  datamart:
